# monica.env.example — copy to monica.env (git-ignored) once Monica is running.
# MONICA_URL  = your Monica base URL (no trailing /api), e.g. http://localhost:8081
# MONICA_TOKEN = a Personal Access Token from Monica → Settings → API
MONICA_URL=
MONICA_TOKEN=
