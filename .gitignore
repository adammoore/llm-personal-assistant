# === Secrets — NEVER commit (Phase 0 hygiene) ===
.env
.env.*
!.env.example
token.json
client_secret.json
*credentials*.json
*secret*.json
*.pem
*.key

# === Phase 0 local-only artifacts (contain secret values / bulk) — never commit ===
PHASE0_SECURITY_REPORT.md

# === Schedule config (may hold phone numbers / send commands) ===
schedule/nudge.env

# === Monica CRM API config (URL + token) ===
monica.env

# === Local data / private content ===
data/          # private assistant data (daily notes, task store) — keep out of git
*.sqlite3
*.db

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# === Node ===
node_modules/
npm-debug.log
yarn-error.log

# === IDEs / OS ===
.idea/
.vscode/
.DS_Store
*.swp

# === Logs ===
*.log

# calevent cross-process lock (runtime, not source)
data/*.lock
